<!DOCTYPE html>
<html lang="tr" class="no-js">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% set siteName = site.name or 'Akyol Hukuk Bürosu' %}
    {% set computedTitle = (title ~ ' | ' ~ siteName) if title else (siteName ~ ' | ' ~ site.founder) %}
    {% set description = metaDescription or site.description %}
    {% set canonicalPath = page.url | url %}
    {% set canonicalUrl = (site.url or '') + canonicalPath %}
    <title>{{ computedTitle }}</title>
    <meta name="description" content="{{ description }}">
    <meta name="theme-color" content="#102422">
    <link rel="canonical" href="{{ canonicalUrl }}">

    <meta property="og:type" content="website">
    <meta property="og:title" content="{{ computedTitle }}">
    <meta property="og:description" content="{{ description }}">
    <meta property="og:url" content="{{ canonicalUrl }}">
    <meta property="og:image" content="{{ site.url }}/images/hero-bg-1.jpg">
    <meta property="og:locale" content="tr_TR">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ computedTitle }}">
    <meta name="twitter:description" content="{{ description }}">
    <meta name="twitter:image" content="{{ site.url }}/images/hero-bg-1.jpg">

    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Source+Sans+3:wght@400;500;600;700&display=swap&subset=latin-ext" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-dymHY7NpC4lMOBBxjAq2c/5+0aP0xSe3UwDQoq7EjiWxZmf0RduCMJLZemfCcqcg05f85QByOk7RwwN7gBl6Ug==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/style.css">

    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "LegalService",
        "name": "{{ site.name }}",
        "image": "{{ site.url }}/images/hero-bg-1.jpg",
        "url": "{{ site.url }}",
        "telephone": "{{ site.phone }}",
        "email": "mailto:{{ site.email }}",
        "founder": {
            "@type": "Person",
            "name": "{{ site.founder }}"
        },
        "address": {
            "@type": "PostalAddress",
            "streetAddress": "{{ site.address.street }}",
            "addressLocality": "{{ site.address.district }}",
            "addressRegion": "{{ site.address.region }}",
            "addressCountry": "TR",
            "postalCode": "{{ site.address.postalCode }}"
        },
        "areaServed": "İstanbul"
    }
    </script>
</head>
<body>
    <script>document.documentElement.classList.replace('no-js','js');</script>
    <a class="skip-link" href="#main-content">İçeriğe atla</a>
    <header class="site-header" data-header>
        <div class="container site-header__inner">
            <a href="/" class="site-brand" aria-label="{{ site.name }} ana sayfası">
                <img src="/images/logo.png" alt="{{ site.name }} logosu" class="site-logo" width="160" height="64" loading="lazy">
                <div class="site-brand__text">
                    <span class="site-brand__title">{{ site.name }}</span>
                    <span class="site-brand__subtitle">{{ site.founder }}</span>
                </div>
            </a>
            <button type="button" class="site-header__toggle" aria-controls="primary-navigation" aria-expanded="false" data-nav-toggle>
                <span class="sr-only">Menüyü aç</span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </button>
            <nav id="primary-navigation" class="site-nav" data-nav aria-label="Ana menü">
                <ul class="site-nav__list" role="list">
                    {% for item in navigation %}
                    <li class="site-nav__item"><a class="site-nav__link" href="{{ item.url }}" data-nav-link>{{ item.label }}</a></li>
                    {% endfor %}
                </ul>
                <a class="btn btn--small btn--ghost site-nav__cta" href="tel:{{ site.phone }}">Hemen Ulaşın</a>
            </nav>
        </div>
    </header>

    <main id="main-content">
        {{ content | safe }}
    </main>

    <footer class="site-footer">
        <div class="container site-footer__inner">
            <div class="site-footer__upper">
                <div class="site-footer__brand">
                    <div class="site-footer__emblem" aria-hidden="true">
                        <img src="/images/typewriter-mark.svg" alt="">
                    </div>
                    <div>
                        <p class="site-footer__title">{{ site.name }}</p>
                        <p class="site-footer__subtitle">{{ site.founder }}</p>
                        <p class="site-footer__description">{{ site.description }}</p>
                    </div>
                </div>
                <div class="site-footer__columns">
                    <div class="site-footer__column">
                        <p class="site-footer__heading">Hızlı Erişim</p>
                        <ul class="site-footer__nav" role="list">
                            {% for item in navigation %}
                            <li><a href="{{ item.url }}">{{ item.label }}</a></li>
                            {% endfor %}
                        </ul>
                    </div>
                    <div class="site-footer__column">
                        <p class="site-footer__heading">İletişim</p>
                        <ul class="site-footer__nav" role="list">
                            {% for channel in contact.channels %}
                            <li>
                                <span class="site-footer__label">{{ channel.label }}:</span>
                                {% if channel.url %}
                                <a href="{{ channel.url }}">{{ channel.value }}</a>
                                {% else %}
                                <span>{{ channel.value }}</span>
                                {% endif %}
                            </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </div>
            <div class="site-footer__lower">
                <div class="site-footer__meta">
                    <address class="site-footer__address">
                        {{ site.address.street }}<br>
                        {{ site.address.district }}, {{ site.address.region }} / {{ site.address.city }} {{ site.address.postalCode }}
                    </address>
                    <div class="site-footer__contact-links">
                        <a href="tel:{{ site.phone }}">Telefon: {{ site.phone }}</a>
                        <a href="mailto:{{ site.email }}">E-posta: {{ site.email }}</a>
                    </div>
                </div>
                <p class="site-footer__legal">&copy; {{ global.currentYear }} {{ site.name }}. Tüm hakları saklıdır.</p>
            </div>
        </div>
    </footer>

    <button type="button" class="scroll-top-btn" data-scroll-top aria-label="Sayfanın başına dön">
        <i class="fas fa-arrow-up" aria-hidden="true"></i>
    </button>

    <div class="modal" data-modal hidden>
        <div class="modal__overlay" data-modal-overlay></div>
        <div class="modal__dialog" role="dialog" aria-modal="true" aria-labelledby="contact-modal-title">
            <button type="button" class="modal__close" data-modal-close aria-label="İletişim formunu kapat">
                <span aria-hidden="true">&times;</span>
            </button>
            <h2 id="contact-modal-title">İletişim Formu</h2>
            <p class="modal__intro">Aşağıdaki formu doldurarak hukuki ihtiyacınızı özetleyebilir, geri dönüş için tercih ettiğiniz kanalı belirtebilirsiniz.</p>
            <form id="contact-form" action="https://formspree.io/f/xanplqpo" method="POST" class="contact-form">
                <label for="name">Adınız Soyadınız</label>
                <input type="text" id="name" name="name" autocomplete="name" required>

                <label for="email">E-posta Adresiniz</label>
                <input type="email" id="email" name="email" autocomplete="email" required>

                <label for="phone">Telefon Numaranız (İsteğe bağlı)</label>
                <input type="tel" id="phone" name="phone" autocomplete="tel">

                <label for="subject">Konu</label>
                <input type="text" id="subject" name="subject" required>

                <label for="message">Mesajınız</label>
                <textarea id="message" name="message" rows="6" required></textarea>

                <button type="submit" class="btn btn--primary form-submit-btn">Formu Gönder</button>
                <p id="form-status" class="form-status" role="status" aria-live="polite"></p>
            </form>
        </div>
    </div>

    <script src="/js/main.js" defer></script>
</body>
</html>
